[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "earshot"
version = "0.1.0"
description = "Conversation recorder for Raspberry Pi — captures audio and uploads to API for transcription"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "tomli-w",
    "httpx",
]

[project.optional-dependencies]
# Raspberry Pi: ReSpeaker capture, APA102 LED, GPIO button (see ADR 0007).
pi = [
    "pyaudio",
    "apa102-pi",
    "RPi.GPIO",
    "spidev",
]

[tool.setuptools.packages.find]
where = ["."]
include = ["earshot*"]
